
/*let liEls2 = document.querySelectorAll('.itens-price ul li');

let index2 = 0;

const show1 = increase => {
  index2 = index2 + increase;
  index2 = Math.min(
    Math.max(index2,0),
    liEls2.length-1
  );
  liEls2[index2].scrollIntoView({behavior: 'smooth'});
}
/*window.show1 = function(increase) {
  index2 = index2 + increase;
  index2 = Math.min(
    Math.max(index2,0),
    liEls2.length-1
  );
  liEls2[index2].scrollIntoView({behavior: 'smooth'});
}*/