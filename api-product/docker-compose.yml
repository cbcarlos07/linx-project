version: "3"

services: 
    product:
        build: .
        command: npm run dev
        container_name: api_product
        ports: 
            - 4000:4000
        volumes: 
            - .:/usr/app
networks:
    default:
        external:
            name: linx-network
            
